{% extends 'base.html.twig' %}

{% block body %}
    index

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Send">
    {{ form_end(form) }}

    <a href="{{ path('adminLogout') }}">Wyloguj się</a>

{% endblock %}